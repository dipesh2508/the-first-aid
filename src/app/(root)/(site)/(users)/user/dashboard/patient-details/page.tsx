import PatientDetails from "@/components/form/PatientDetails";
import React from "react";

const page = ({}) => {
  return (
    <div>
      <PatientDetails />
    </div>
  );
};

export default page;
